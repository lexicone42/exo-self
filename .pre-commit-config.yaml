repos:
  - repo: local
    hooks:
      - id: sync-version
        name: sync version to marketplace.json + plugin.json
        language: system
        entry: ./scripts/sync-version.sh
        always_run: true
        pass_filenames: false
